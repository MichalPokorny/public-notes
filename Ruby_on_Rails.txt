- bootstrap →
	Bootswatch - temata pro Bootstrap

- Selenium
- Cucumber
- SimpleNavigation
- CanCan

Views -> Cells

rails s + gem debugger
-> kdekoliv dam breakpoint pomoci "debugger".

- rake -T: list tasks

- Jenkins continuous integration

- Postgres: umi se chovat jako NoSQL pres HSTORE

- rails-recurly-subscription-saas

- hodilo by se umet psat rychlejsi testy.

(Pry REPL)

- Model.scoped misto Model.all

- Rails - mozna umi neco jako info

- pdfkit: html -> pdf (asi pres virtualni browser? :)

- rails log tagging

- bundle install --deployment

- template inlining - zrychleni

- capistrano: set :use_sudo, false

- vytvoreni noveho gemu: bundle gem

- bootstrap-sass: config.assets.precompile += %w(*.png *.jpg *.jpeg *.gif) >>> neni to potreba???

- .gemrc: gem: --local
- bundle --path /home/prvak/.gem/ruby/2.0.0

- integracni testy jsou zajimave

- cap -s branch=... deploy

- ruby-prof -p graph_html -f profil.html, a vubec: ruby-prof (je to gem)

- gem phony: validace a standardizace telefonnich cisel

- bundler ma od nejake docela moderni verze flag (-j) --- umi paralelizovat!

- cap deploy:migrations: podobne jako deploy, ale sebehnou i migrace

- ruby -run -e httpd -- [OPTION] DocumentRoot

- yard server -g

rails bundler require: odebrat ze skeletonu!?

interpolace symbolu funguje i bez explicitniho prevodu na string

- active extensions: extract_options!
