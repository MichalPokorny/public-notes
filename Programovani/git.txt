git notes edit (sha1): poznamky ke commitu — jde to automatizovat, ale nejde to efektivne sdilet (hodi se pro CI atd.)

git filter-branch --index-filter 'git rm --cached --ignore-unmatch MujCitlivySoubor' \
  --prune-empty --tag-name-filter cat -- --all

	(=git purge-file)

**Konfigurace:**
	(barvicky)
	[merge]
		tool = vimdiff
	[push]
		default = upstream

	(rozumnejsi format logu)
	[log]
		abbrevCommit = true
		decorate = short
		date = local
	[format]
		pretty = short

	(min ukecanosti)
	[advice]
		pushNonFastForwards = false
		statusHints = false
		commitBeforeMerge = false

	(aby nerval gitk)
	[gui]
		spellingdictionary = none
